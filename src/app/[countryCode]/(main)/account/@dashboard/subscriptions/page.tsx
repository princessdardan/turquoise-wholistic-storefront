import { notFound } from "next/navigation"

// Subscriptions feature is currently disabled.
// To re-enable, restore the original page content from git history.
export default function Subscriptions() {
  notFound()
}
